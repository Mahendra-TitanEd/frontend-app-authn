import { defineMessages } from '@edx/frontend-platform/i18n';

export default defineMessages({
  closeDialog: {
    id: 'custom.popupDialog.close',
    defaultMessage: 'Close dialog',
    description: 'Accessible label for closing a popup dialog',
  },
});
