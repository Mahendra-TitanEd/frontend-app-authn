import { defineMessages } from '@edx/frontend-platform/i18n';

export default defineMessages({
  'AUTH.BACK_TO_LOGIN': {
    id: 'AUTH.BACK_TO_LOGIN',
    defaultMessage: 'Back to Login',
    description: 'Custom auth — link back to login page',
  },
});
